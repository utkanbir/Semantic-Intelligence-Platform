apiVersion: v1
kind: Secret
metadata:
  name: sip-backend-secrets
type: Opaque
stringData:
  # Template values only. Replace at deploy time.
  SIP_DATABASE_URL: postgresql://sip_user:replace-me@sip-postgres:5432/sip_db
