apiVersion: v1
kind: Secret
metadata:
  name: sip-minio-secrets
type: Opaque
stringData:
  # Template values only. Replace before shared deployments.
  MINIO_ROOT_USER: sip_minio
  MINIO_ROOT_PASSWORD: replace-me
