apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sip-dev

resources:
  - ../../base

labels:
  - pairs:
      environment: dev

# S13: pin console image after Blueprint screen (TD-016 pattern)
images:
  - name: sip-console:dev
    newName: sip-console
    newTag: s14
